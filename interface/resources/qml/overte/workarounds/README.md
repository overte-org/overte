These are to work around the current desktop system, which requires
components to be wrapped in a Window to have a draggable border and titlebar.

This isn't required on the tablet because it doesn't support multiple windows at once.
